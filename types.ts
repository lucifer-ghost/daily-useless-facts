export interface Fact {
  id: string;
  text: string;
}

export interface Testimonial {
  id: number;
  name: string;
  role: string;
  quote: string;
  avatar: string;
}

export interface StatItem {
  label: string;
  value: string;
  description: string;
}
